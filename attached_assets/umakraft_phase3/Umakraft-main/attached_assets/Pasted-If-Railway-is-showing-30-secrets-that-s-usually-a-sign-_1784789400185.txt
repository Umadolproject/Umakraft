If Railway is showing **30+ secrets**, that's usually a sign that the project has accumulated configuration for many different providers over time. If you're moving to a **single self-hosted Hugging Face service**, most of those can likely be removed.

You can ask Replit to perform a full cleanup rather than just removing API keys.

Perform a complete environment variable audit and cleanup for this project.

The project is being migrated to a self-hosted Hugging Face Transformers service running in the same Railway project.

Goals:

* Find every `process.env` reference in the codebase.
* Remove every environment variable that is no longer used.
* Remove all AI provider API keys (OpenAI, Gemini, Groq, Together AI, Anthropic, OpenRouter, Hugging Face Inference API, etc.).
* Remove duplicate, legacy, or unused secrets.
* Move non-sensitive values (Application ID, Guild ID, ports, URLs, service names, feature flags, etc.) into configuration files or constants instead of Railway secrets.
* Use Railway's internal networking for communication with the self-hosted AI service.
* Eliminate all AI authentication logic that is no longer needed.
* Refactor the code so there are no references to deleted environment variables.
* Delete dead code and unused dependencies related to removed providers.
* Update `.env.example`, README, and deployment documentation.

After the refactor, the only required Railway secret should be:

* `DISCORD_TOKEN`

Everything else should either be:

* Removed because it is unused.
* Stored in configuration because it is not sensitive.
* Obtained automatically through Railway's internal networking.

Finally, provide:

1. Every removed environment variable.
2. Every remaining environment variable.
3. Every modified file.
4. Confirmation that there are no orphaned `process.env` references anywhere in the project.

One thing to double-check, though: **30+ Railway secrets doesn't necessarily mean they're all being used.** Many projects end up with old variables from previous deployments, experiments, or templates. Before deleting them from Railway, it's a good idea to let Replit scan the codebase and identify which `process.env` variables are actually referenced. That way you won't accidentally remove something the bot still depends on.
